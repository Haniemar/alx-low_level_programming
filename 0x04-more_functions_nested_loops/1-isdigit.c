#include "main.h"
/**
 * _isdigit - check for digit 0 through 9
 * @c: input integer
 * Return: 1 if digit 0 otherwise
 */
int _isdigit(int c)
{
	int i;

	c = 0;
	if (i >= 0 && i <= 9)
	{
		c = 1
	}
	return (c);
}
